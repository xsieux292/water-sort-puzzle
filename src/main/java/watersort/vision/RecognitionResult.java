package watersort.vision;

import watersort.model.BoardState;

import java.util.List;
import java.util.Map;

/**
 * ผลลัพธ์จากการ recognize ภาพหน้าจอเกม
 *
 * ประกอบด้วย:
 * - BoardState ที่ extract ได้ (null ถ้า invalid)
 * - ตำแหน่งของหลอดในภาพต้นฉบับ
 * - Color mapping (ID → ชื่อสี)
 * - ค่าความมั่นใจ (confidence)
 * - Warnings / Errors
 *
 * @see SPEC.md Section 9.6
 */
public class RecognitionResult {

    // TODO: Implement fields and methods
    // ดู data structure ใน SPEC.md Section 9.6

    // --- Fields ---
    // BoardState boardState;
    // List<TubeRegion> tubeRegions;
    // Map<Integer, String> colorMap;
    // double confidence;
    // List<String> warnings;
    // List<String> errors;
    // boolean isValid;
}
